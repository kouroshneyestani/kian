// components
import Events from "@/components/Events";
import EventsNavbar from "@/components/EventsNavbar";

const Page: React.FC = () => {
    return (
        <>
            <EventsNavbar />
            <Events />
        </>
    );
};

export default Page;
